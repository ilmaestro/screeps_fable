module Action.Idle
open System
open Fable.Core
open Fable.Core.JsInterop
open Fable.Import
open Helpers

(*
    TODOs:
    - fallback action, if a creep cannot find anything to do, goto Idle flag area and wait.
*)